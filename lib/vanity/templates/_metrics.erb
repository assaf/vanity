<ul class="metrics">
  <% metrics.sort_by { |id, metric| Vanity::Metric.name(id, metric) }.each do |id, metric| %>
    <li class="metric" id="metric<%=h id.to_s %>">
      <%= render Vanity.template("metric"), id: id, metric: metric %>
    </li>
  <% end %>
</ul>
